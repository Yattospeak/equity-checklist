---
name: equity-checklist
description: "麦可席恩投资检查表分析技能。**必须**在用户输入中包含「检查表」（或「投资检查表」「麦可席恩检查表」「checklist」）关键词且附带具体企业（名称/简称/代码）时才触发，例如「用投资检查表技能分析贵州茅台」「检查表：分析600519」。普通「分析一下XX」不触发本技能。自动采集财报/行情/公告，按四维模型（业务30%/管理层25%/财务25%/估值20%）生成中文 HTML 报告，含排雷信号、规则A/B闸门。关键词：投资检查表、检查表分析、麦可席恩、checklist"
version: "2.3.0"
author: "WorkBuddy"
created: "2026-09-05"
updated: "2026-09-13"
---

# Equity Checklist（麦可席恩投资检查表 · 强化版）

> **Goal**：让用户在微信 / 小程序 / Web 端输入 **「检查表 + 企业名称/代码」**，**秒级获得一份基于公开财报数据的专业检查表分析报告（HTML）**。

## When to Use（严格触发条件）

**本技能只在用户输入同时满足以下两个条件时触发：**

1. **包含「检查表」关键词**（中英文皆可）：`检查表` / `投资检查表` / `麦可席恩检查表` / `checklist`
2. **附带明确标的**：股票名称 / 简称 / 拼音 / 代码

### 合法触发示例

| 用户输入 | 是否触发 | 解析结果 |
|---------|:---:|------|
| 用投资检查表技能分析贵州茅台 | ✅ | 贵州茅台（600519.SH） |
| 投资检查表：分析 600519 | ✅ | 贵州茅台（600519.SH） |
| 检查表分析歌华有线 | ✅ | 歌华有线（600037.SH） |
| 用麦可席恩检查表看看比亚迪 | ✅ | 比亚迪（002594.SZ） |
| equity-checklist analyze 腾讯控股 | ✅ | 腾讯控股（00700.HK） |
| 帮我跑一下 600519 的 checklist | ✅ | 贵州茅台（600519.SH） |

### 触发后的处理规则

- **只有关键词没有标的**（如「用检查表分析一下」）→ **不直接执行**，先追问「你想分析哪家公司？输入名称或代码即可」
- **只有标没有关键词**（如「分析一下贵州茅台」）→ **不触发本技能**，转由通用行情/个股分析技能处理

## When NOT to Use（重要）

以下场景**严禁**触发此技能：

- 用户输入**不含「检查表」关键词**的任何分析请求（如「分析一下茅台」「帮我看看这只股」）
- 用户要求**实时行情盯盘**、当日 K 线、分时走势（应使用 westockdata / a-stock-data 行情 skill）
- 用户要求**买卖建议 / 仓位管理 / 择时**（合规限制，本技能是研究工具而非投资顾问）
- 用户已提供**完整的研究报告**，要求基于此做二次加工
- 用户输入仅有关键词**无具体标的**，且追问后仍不提供 → 不执行

**覆盖市场**：A 股（沪市 / 深市 / 北交所）完整支持；港股（00700、09992 等 5 位代码）支持行情 + 估值，财务维度显示「数据不足」。

## Core Principles

1. **数据驱动**：所有指标基于公开财报自动计算，可复核、可回溯，**不输出主观结论而不给依据**
2. **多层可信度标注**：财务 / 估值维度自动分可信（数据驱动）；业务 / 管理层维度对**诚信度、内部人持股、薪酬对齐**等需要联网核查的项一律给中性 3.0 并显式标注「🔍 待人工核查」
3. **数据不足时不误导**：港股财报缺失时显示「数据不足」而非给出虚假综合分；金融股（银行 / 保险 / 证券）的实业口径指标自动禁用并显著警告
4. **合规边界**：报告固定附带免责声明；不输出"买入 / 卖出 / 目标价"等投资顾问意见，仅输出综合评分与维度分析供用户研究参考

## 评分模型（v2.3 · 麦可席恩检查表 · 强化版）

| 维度 | 权重 | 自动分可信度 | 评分依据 |
|------|:----:|:----:|------|
| 业务质量 | 30% | 中（代理指标） | ROE、毛利率、增速 |
| 管理层 | 25% | 低（仅资本配置可量化） | 资本配置记分卡 30 分制 + 诚信/内部人/薪酬中性 |
| 财务质量 | 25% | **高（纯数据）** | OCF/净利、资产负债率、ROE、商誉 |
| 估值 | 20% | 中 | PE、PB、股息率（无历史分位时偏保守） |

**硬性闸门**：任一维度 <3.0 → 结论降级为「低」。

**评级档位（三档）**：综合分 ≥3.8 → **优**；3.0–3.8 → **中**；<3.0 或触发硬性闸门 → **低**。
输出评级而非买卖建议——本工具不是投资顾问。

**规则 A**（管理层风险标注）：管理层任一子维度 ≤2 → ⚠️ 标记 + 置信度下调至"低"。
**规则 B**（一票否决）：并购暴雷 + 再融资圈钱 + 监管处罚 三瑕疵同现 → 管理层封顶 2.5。

完整标准见技能根目录 `检查表评估模型.md`（注意：不在 references/ 子目录）。

## 排雷信号（自动检测）

| 类别 | 信号 | 阈值 |
|------|------|------|
| **财务** | 商誉/净资产过高 | ≥20% 高，≥10% 中 |
| **财务** | OCF/净利连续 2 年 < 1 | 含金量失真 |
| **财务** | 投资收益占营业利润 ≥ 50% | 利润结构失真 |
| **财务** | 净利连续 2 年下滑 | 趋势恶化 |
| **财务** | 资产负债率 ≥ 70% | 高杠杆（金融股禁用） |
| **回报** | 股息率 < 1% 且无回购 且账上净现金 | 股东回报缺失 |
| **治理** | 近期有再融资 / 募资公告 | 须核查闲置率 |
| **治理** | 减持 / 监管处罚 / 业绩承诺 | 须人工核查 |
| **正面** | 存在回购 / 增持 / 长期分红 | 资本配置正面信号 |

## 工作流

```
用户输入
   ↓
0. 触发校验（强制）：
   ├─ 输入含「检查表」关键词？  ──否──→ 拒绝执行，转交其他分析技能
   ├─ 附带了明确标的？          ──否──→ 追问「想分析哪家公司？」
   ├─ 标的可解析？              ──否──→ 反馈无法解析，请用户换名称/代码
   └─ 全部通过 → 继续
   ↓
1. datasource.resolve_symbol()  → {market, code, secucode}
   ↓
2. 行情 + 财报三表 + 分红 + 公告（多源冗余，限流 1.2s）
   ↓
3. collect.collect()            → 计算指标与排雷信号（含行业适用性判定）
   ↓
4. auto_score.auto_score()      → 规则化自动打分（基准分）
   ↓
5. render.render_report()       → 生成移动端优先的 HTML
   ↓
输出单文件 HTML（含内联 CSS，零外部依赖，微信 webview / 小程序可直接展示）
```

### 单一命令一键出报告

```bash
python3 scripts/analyze.py "贵州茅台" -o /tmp/report.html
```

完成后 HTML 可直接：

- 微信内置浏览器打开 / 发送给朋友
- 小程序 `<web-view>` 标签加载本地文件
- 浏览器直接打开
- 上传到 CDN 后微信小程序可远程加载

### 三阶段调用（更适合 LLM 深度参与场景）

```bash
# 阶段 1：采集（耗时 4-30s；-o 即完整 metrics JSON，无需额外 flag）
python3 scripts/collect.py "贵州茅台" -o metrics.json

# 阶段 2：基于 LLM 判断的四维打分（也可自动）
python3 scripts/auto_score.py metrics.json -o scores.json
# 或手写 scores.json（含 dimensions, management_detail, summary, thesis, risks, watchlist）

# 阶段 3：渲染
python3 scripts/render.py metrics.json --scores scores.json -o report.html
```

LLM 在第二阶段可补充：

- 业务维度：基于行业认知评估护城河深度
- 管理层维度：联网核查诚信度 / 减持 / 监管处罚
- 估值维度：接入历史分位
- 输出摘要 / 核心逻辑 / 风险 / 跟踪清单

## 数据源（已验证可用，无需 API Key）

| 域 | 主源 | 备源 | 说明 |
|----|------|------|------|
| 名称→代码 | 腾讯 smartbox | — | 支持 A股/港股/美股 |
| 实时行情 | 腾讯 qt.gtimg.cn | — | A股 + 港股 |
| 财报三表（A股） | 新浪 CompanyFinanceService | 东财 DMSK | 新浪字段全含商誉/合同负债；东财规范可批量 |
| 财报（港股） | 东财 HKF10（`RPT_HKF10_FN_*`） | — | 纵表结构 + 中国香港准则科目，已在 datasource 层转置并对齐 A 股口径 |
| 财务摘要 | 东财 DMSK | — | 扣非净利补全 + 行业名识别 |
| 分红 | 东财 RPT_SHAREBONUS_DET | — | 注意单位是每 10 股 |
| 股东户数 | 东财 RPT_HOLDERNUMLATEST | — | |
| 公告 | 巨潮 cninfo | — | 关键词匹配排雷信号 |

## 行业适用性

- **普通实业**：完整适用
- **金融业（银行 / 保险 / 证券 / 多元金融）**：自动识别并禁用净现金、有息负债、资产负债率、OCF/净利、投资收益/营业利润、毛利率、净利率等**实业口径下失真的指标**。报告中显著标注「金融行业请用专用框架复核」。
- **港股**：**利润表与资产负债表已接入**（东财 HKF10，实测可回溯 9 个年度），
  业务 / 财务 / 估值维度可正常评分。以下三项无免登录公开源，按**缺失**处理
  （中性计分，绝不按 0 分处理）：现金流量表、分红、股东户数。
  报告会在显著位置标注【港股数据边界】。

## 已知限制

- 港股**无现金流量表**源 → OCF/净利、自由现金流等指标恒为缺失，盈利含金量项按中性计
- 港股**无分红 / 股东户数 / 公告**源 → 股息率、派息率、资本配置中的分红项缺失，估值偏保守
- 港股数据源为纵表且单页有行数上限，采集耗时约 35–40s（A 股约 8s）
- 历史估值分位（PE/PB 10 年分位）未自动算，需手动 LLM 补充
- 管理层诚信度 / 减持 / 监管处罚需联网核查，脚本标"待人工核查"
- 数据源为公开免费源，频率敏感，限流 1.2s + 重试 2 次
- 商业化前建议采购 Wind / 聚源 / 财汇 等授权源

## 合规与免责声明

- 本技能是**研究工具**而非投资顾问，不输出明确的「买入 / 卖出 / 目标价」建议
- 报告输出综合评分与维度分析，仅供研究参考
- 数据采集自公开源，请复核
- 报告固定附带「免责声明」段落

## 输出示例

参见 `examples/` 目录：

- `贵州茅台_600519_检查表分析报告.html` — 「优」评级案例（4.36/5.0）
- `歌华有线_600037_检查表分析报告.html` — 「低」评级案例（2.35/5.0，规则 A 触发）
- `腾讯控股_00700_检查表分析报告.html` — 港股案例（财报已接入，现金流/分红仍缺）